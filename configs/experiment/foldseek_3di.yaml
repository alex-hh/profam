# @package _global_

# to execute this experiment run:
# python train.py experiment=foldseek_small

defaults:
  - /extra_callbacks@extra_callbacks.esmfold: esmfold
  - override /callbacks: default_with_shuffle
  - override /data: foldseek
  - override /model: llama_tiny

tags: ["pfam"]

seed: 12345

extra_callbacks:
  esmfold:
    pipeline:
      evaluation_parquet: ${data.dataset_configs.foldseek_val.data_path_pattern}

trainer:
  min_epochs: 10
  max_epochs: 10000
  gradient_clip_val: 1.0
  val_check_interval: 0.1  # TODO: set this appropriately...
  check_val_every_n_epoch: 1
  num_sanity_val_steps: 2

model:
  scheduler_name: "inverse_sqrt"  # inverse_sqrt, cosine, ...
  num_warmup_steps: 1000
  scoring_max_tokens: 10240

data:
  batch_size: 2
  num_workers: 12
  evaluate_gym: True
  evaluate_ec_class: True
  evaluate_ec_cluster_class: True

logger:
  wandb:
    tags: ${tags}
    group: "foldseek"

float32_matmul_precision: high
pre_evaluate_gym: false  # use to run evaluation first to check for issues
pre_evaluate_ec_class: false
