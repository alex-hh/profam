Extra callbacks are configured and loaded differently.
Because of need for defaults for config composition
- there's probably a better way to handle this
Also difficulty of appending to an existing callbacks list
